<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <!--<item name="colorAccent">@color/colorAccent</item>-->
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ThemeDrawerHeader.Account.Name" parent="@style/AppTheme">
        <item name="android:textStyle">bold</item>
        <item name="android:textSize">@dimen/material_drawer_header_text_22sp</item>
    </style>


    <style name="ThemeDrawerHeader.Account.Email" parent="@style/AppTheme">
        <item name="android:textColor">@color/red_d04f2a</item>
    </style>

    <!-- Fonts -->
    <style name="OpenSansBold" parent="android:TextAppearance">
        <item name="fontPath">fonts/OpenSansBold.ttf"</item>
    </style>

</resources>
